# Session-wide default shell. Login shell is already fish (chsh), but GDM
# exports SHELL=/bin/bash into the graphical session; override it here so
# every app that follows $SHELL (foot, editors, etc.) gets fish.
SHELL=/usr/bin/fish
